#!/usr/bin/env zsh

for process in ${@:1}
do 
	case "$1" in
		"show")
		cmd=windowmap
		;;
		"hide")
		cmd=windowunmap
		;;
	esac
	xdotool search --class $process $cmd %@ windowraise %@ || xdo $1 -N $process
done