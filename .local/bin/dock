#!/bin/sh

dock="polybar lxqt-panel conky"

[[ -z $1 ]] && {
    killall polybar lxqt-panel conky
    sleep 1
    polybar example & lxqt-panel & conky
    sleep 1
    xctl hide $dock
} || {
    xctl $1 $dock
}